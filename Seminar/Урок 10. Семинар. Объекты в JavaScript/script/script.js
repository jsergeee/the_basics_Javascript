
// Задание 1 (тайминг 20 минут)
// Текст задания
// 1. Создайте объект с ключами от 1 до 7, в качестве
// значений содержащий имена дней недели. Выведите на
// экран “Вторник”

// const calendar = {1: 'Понедельник', 2: 'Вторник', 3:'Среда', 4:'Четверг', 5: 'Пятница', 6: 'Суббота', 7: 'Воскресенье'};
// console.log(calendar[2]);



// 2. Создайте объект user с ключами 'name', 'surname', ‘age’.
// Выведите на экран фамилию, имя и возраст через дефис.
// 3. Добавьте в объект user свойство отчество, которое
// пользователь должен ввести с клавиатуры
// 4. Удалите свойство surname


// const user = {fname: 'Serg', surname: 'Yakovenko', age: '47'};
// const patronymic = prompt("Введите отчество:");
// user.patronymic = patronymic;
// console.log(`${user.surname} - ${user.fname} - ${user.age} - ${user.patronymic}`);
// // Удаляем свойство surname
// delete user.surname;
// console.log(`${user.surname} - ${user.fname} - ${user.age} - ${user.patronymic}`);


//==============================================================
// Задание 2 (тайминг 25 минут)
// 1. Даны два массива: первый с названиями дней недели, а второй - с
// их порядковыми номерами:
// const arr1 = ['пн', 'вт', 'ср', 'чт', 'пт', 'сб', 'вс'];
// const arr2 = [1, 2, 3, 4, 5, 6, 7];
// С помощью цикла создайте объект, ключами которого будут
// названия дней, а значениями - их номера.
// 1. const obj = {x: 1, y: 2, z: 3};
// Переберите этот объект циклом и возведите каждый элемент
// этого объекта в квадрат.

// const arr1 = ['пн', 'вт', 'ср', 'чт', 'пт', 'сб', 'вс'];
// const arr2 = [1, 2, 3, 4, 5, 6, 7];

// const daysOfWeek = {};
// for (let i = 0; i < arr1.length; i++) {
//   for (let j = 0; j < arr2.length; j++) {
//     if (i === j) {
//       // Проверяем, чтобы индексы совпадали
//       daysOfWeek[arr1[i]] = arr2[j];
//     }
//   }
// }
// console.log(daysOfWeek);


// // Переберите этот объект циклом и возведите каждый элемент
// // этого объекта в квадрат.


// for (let day in daysOfWeek) {
//   daysOfWeek[day] = daysOfWeek[day] ** 2; // Или можно использовать Math.pow(daysOfWeek[day], 2);
// }

// console.log(daysOfWeek);

//============================================================================
// Задание 3 (тайминг 15 минут) Найдите сумму элементов приведенного объекта.

// const obj = {
// key1: {
// key1: 1,
// key2: 2,
// key3: 3,
// },
// key2: {
// key1: 4,
// key2: 5,
// key3: 6,
// },
// key3: {
// key1: 7,
// key2: 8,
// key3: 9,
// },
// }


// const sum = Object.values(obj).reduce((acc, curr) => {
//   return acc + Object.values(curr).reduce((a, b) => a + b, 0);
// }, 0);

// console.log(sum); // Выводит 45

//Чтобы найти сумму всех элементов в приведенном объекте, можно использовать метод Object.values() для получения значений и затем пройтись по ним с помощью метода reduce(). 
//В этом коде мы сначала получаем массив значений верхнего уровня объекта, затем для каждого из этих значений (которые тоже являются объектами) получаем их значения и суммируем. В итоге мы получаем общую сумму всех чисел. Сумма элементов в данном объекте равна 45.

//=====================================================================================

//  Создайте объект riddles
// 2. Добавьте свойства question, answer
// 3. создайте метод askQuestion который спрашивает у пользователя
// вопрос question и сравнивает ответ с answer
// 4. Если ответил неверно, то в консоль выводится текст: “вы
// проиграли”



